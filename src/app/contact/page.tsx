import ContactContent from "@/components/ContactContent";

export const metadata = {
  title: "Contact",
  description:
    "Ready to restore your treasures? Let us start the conversation.",
};

export default function ContactPage() {
  return <ContactContent />;
}
